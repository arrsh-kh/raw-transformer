Transformers with no external libraries, not even numpy for python.

Currently writing it in python to for learning purposes and then will rewrite the same in C and then optimise it heavily with SIMD to get maximum CPU performance with the lease traning time
